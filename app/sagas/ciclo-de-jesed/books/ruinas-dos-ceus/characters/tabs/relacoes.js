(()=>{const R=window.RS,{E,S}=R,SOC=window.RUINAS_SOCIAL||{relationships:[]};R.charTabs.relacoes=c=>{const a=SOC.relationships.filter(x=>x.from===c.n||x.to===c.n);return `<article class="panel"><h2>Relações</h2>${a.map(rel=>{const other=rel.from===c.n?rel.to:rel.from;return `<div class="row click" data-go="personagem/${S(other)}"><strong>${E(other)}</strong><small>${E(rel.type)} · ${E(rel.state)}</small><p>${E(rel.from===c.n?rel.fromView:rel.toView)}</p></div>`}).join('')||'<p>Sem relação detalhada.</p>'}</article>`};})();
